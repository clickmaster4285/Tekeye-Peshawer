# YOLO26 training and deployment (500-camera surveillance)
ultralytics>=8.0.0
PyYAML>=6.0
opencv-python>=4.8.0
numpy>=1.24.0
tqdm>=4.65.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
requests>=2.31.0

# Optional: enable FAISS gallery search when enrolled vectors >= ML_FACE_FAISS_MIN_EMBEDDINGS
# faiss-cpu>=1.7.4

# License plate OCR (ANPR) — PaddleOCR PP-OCRv5
# GTX 1080 is sm_61; current paddlepaddle-gpu wheels need sm_75+, so use CPU Paddle.
# Use Python 3.11/3.12 (not 3.14). Install paddlepaddle before paddleocr.
paddlepaddle==3.2.2
paddleocr>=3.0.0

# Optional: for deployment / inference
# onnx>=1.14.0
# onnxruntime-gpu>=1.15.0
